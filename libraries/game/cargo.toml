[package]
name = "game"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "game"
path = "src/game.rs"

[dependencies]
iplayers = { path = "../iplayers" }
players = { path = "../players" }
dice = { path = "../dice" }
board = { path = "../board", version = "1.0.0"}
rand = "0.8.4"
prettytable-rs = "0.10.0"
